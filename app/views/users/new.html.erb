<h1>ユーザー作成</h1>
<%= form_for @message, url: "/messages", html: { method: :post} do |f| %>
  <p>氏名 <%= f.text_field :name %></p>
  <p>年齢 <%= f.text_field :age %></p>
  <input type="submit" value="送信">
<% end %>
<%= link_to "一覧画面へ", messages_path %>